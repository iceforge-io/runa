<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!--
    Use the Spring Boot parent to pin Maven plugin versions (compiler, surefire, boot plugin, etc.)
    and keep builds stable across Maven/IDE versions.
  -->
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <!--
      3.3.13 includes fixes for CVE-2024-38807 (fixed in 3.3.3+) and is the latest 3.3.x patch line.
      If you prefer, you can move to 3.4.x+ instead.
    -->
    <version>3.3.13</version>
    <relativePath/>
  </parent>

  <groupId>org.iceforge</groupId>
  <artifactId>runa</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <name>Iceforge Semantic Layer</name>
  <description>Semantic layer service that compiles semantic queries into SQL and various DSL internal query languages and streams Arrow results via Skadi.</description>

  <properties>
    <java.version>21</java.version>
    <jackson.version>2.17.2</jackson.version>
    <dependency-check.version>12.2.0</dependency-check.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!-- Optional: keep Jackson pinned explicitly (Spring Boot also manages Jackson). -->
      <dependency>
        <groupId>com.fasterxml.jackson</groupId>
        <artifactId>jackson-bom</artifactId>
        <version>${jackson.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <!-- WebFlux for streaming proxy of Arrow IPC -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webflux</artifactId>
    </dependency>

    <!-- Validation annotations for request objects -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>

    <!-- YAML parsing for semantic-model.yml -->
    <dependency>
      <groupId>com.fasterxml.jackson.dataformat</groupId>
      <artifactId>jackson-dataformat-yaml</artifactId>
    </dependency>

    <!-- Optional: Actuator -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>

    <!-- Tests -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
      <exclusions>
        <exclusion>
          <groupId>org.junit.vintage</groupId>
          <artifactId>junit-vintage-engine</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <dependency>
      <groupId>io.projectreactor</groupId>
      <artifactId>reactor-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.squareup.okhttp3</groupId>
      <artifactId>mockwebserver</artifactId>
      <version>4.12.0</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <release>${java.version}</release>
        </configuration>
      </plugin>

      <!-- Optional: silence ByteBuddy/Mockito inline dynamic-agent warnings on newer JDKs -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <argLine>-XX:+EnableDynamicAgentLoading</argLine>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <!--
    Security scanning (opt-in):
      mvn -Psecurity-scan verify
    Note: first run downloads NVD/OSV data and may take a while.
  -->
  <profiles>
    <profile>
      <id>security-scan</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.owasp</groupId>
            <artifactId>dependency-check-maven</artifactId>
            <version>${dependency-check.version}</version>
            <executions>
              <execution>
                <id>dependency-check</id>
                <goals>
                  <goal>check</goal>
                </goals>
              </execution>
            </executions>
            <configuration>
              <!-- Fail the build on high/critical findings. Tune as desired. -->
              <failBuildOnCVSS>7.0</failBuildOnCVSS>

              <!-- Include test scope so issues in test dependencies are visible. -->
              <skipTestScope>false</skipTestScope>

              <!-- Reports -->
              <format>ALL</format>
              <outputDirectory>${project.build.directory}</outputDirectory>

              <!--
                If you need suppressions, add a file and uncomment the line below:
                <suppressionFile>${project.basedir}/dependency-check-suppressions.xml</suppressionFile>
              -->
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
